services:
  meet-control-server:
    build:
      context: ./meet-control-server
    environment:
      NODE_ENV: production
      PORT: "3001"
    volumes:
      - meet-bot-data:/app/tmp:ro
    ports:
      - "3001:3001"
    restart: unless-stopped
    init: true
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:3001/health').then((response) => process.exit(response.ok ? 0 : 1)).catch(() => process.exit(1))"
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  meet-bot:
    build:
      context: ./meet-bot
    command: ["npm", "run", "start:supervisor"]
    depends_on:
      meet-control-server:
        condition: service_healthy
    environment:
      NODE_ENV: production
      PORT: "3000"
      CONTROL_BASE_URL: http://meet-control-server:3001
      COMMAND_LONG_POLL_MS: "1000"
      CHAT_POLL_INTERVAL_MS: "250"
      CAPTION_POLL_INTERVAL_MS: "250"
      ENABLE_AUDIO_INPUT: "true"
      AUDIO_SEGMENT_DURATION_MS: "1200"
      AUDIO_SEGMENT_COOLDOWN_MS: "50"
      MEET_AUDIO_INPUT_FORMAT: pulse
      MEET_AUDIO_INPUT_DEVICE: meetbot_sink.monitor
      ENABLE_VIDEO_INPUT: "true"
      CAPTURE_VIDEO_FRAMES: "true"
      ENABLE_VIDEO_ACTIVITY: "false"
      VIDEO_POLL_INTERVAL_MS: "250"
      VIDEO_JPEG_QUALITY: "35"
      STT_COMMAND: "python3 /app/scripts/faster_whisper_transcribe.py {input}"
      FASTER_WHISPER_MODEL: base
      FASTER_WHISPER_DEVICE: cpu
      TTS_BACKEND: piper
      PIPER_PATH: /opt/piper/piper
      PIPER_MODEL: /models/piper/en_US-lessac-medium.onnx
      SPEECH_DELIVERY_MODE: meeting-microphone
      AUDIO_OUTPUT_BACKEND: paplay
      AUDIO_OUTPUT_DEVICE: meetbot_sink
      MEET_AUDIO_SINK_NAME: meetbot_sink
      MEET_AUDIO_SOURCE_NAME: meetbot_mic
      MEET_AUDIO_SINK_DESCRIPTION: MeetBot_Virtual_Sink
      MEET_AUDIO_SOURCE_DESCRIPTION: MeetBot_Virtual_Microphone
      MEET_PREFERRED_MICROPHONE_LABEL: MeetBot_Virtual_Microphone
      REALTIME_PUBLIC_HOST: "${REALTIME_PUBLIC_HOST:-localhost}"
      REALTIME_AGENT_AUDIO_RTP_PORT: "${REALTIME_AGENT_AUDIO_RTP_PORT:-5004}"
      REALTIME_AGENT_AUDIO_SAMPLE_RATE: "${REALTIME_AGENT_AUDIO_SAMPLE_RATE:-24000}"
      REALTIME_AGENT_AUDIO_CHANNELS: "${REALTIME_AGENT_AUDIO_CHANNELS:-1}"
      REALTIME_MEETING_AUDIO_RTP_URL: "${REALTIME_MEETING_AUDIO_RTP_URL:-}"
      REALTIME_MEETING_AUDIO_SAMPLE_RATE: "${REALTIME_MEETING_AUDIO_SAMPLE_RATE:-16000}"
      REALTIME_MEETING_AUDIO_CHANNELS: "${REALTIME_MEETING_AUDIO_CHANNELS:-1}"
      REALTIME_VIDEO_RTP_URL: "${REALTIME_VIDEO_RTP_URL:-}"
      REALTIME_VIDEO_FPS: "${REALTIME_VIDEO_FPS:-4}"
    ports:
      - "3000:3000"
      - "5004:5004/udp"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    shm_size: "2gb"
    volumes:
      - meet-bot-data:/app/tmp
    restart: unless-stopped
    init: true
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:3000/health').then((response) => process.exit(response.ok ? 0 : 1)).catch(() => process.exit(1))"
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

volumes:
  meet-bot-data:
